@model List<Everlast.Models.Party>

<div class="v-cover-section">
    <div class="row v-cover-section-content">
        <div class="col-lg-12 text-center">
            <h1 class="display-1">Events</h1>
            <h2 class="v-header">Come join us.</h2>
        </div>
    </div>
</div>

@foreach (Everlast.Models.Party item in Model)
{
    <div class="row">
        <div class="col-lg-12">
            <div class="card v-card text-center">
                <div class="card-header">
                    @item.Title
                </div>
                <div class="card-body">
                    <h5 class="card-title">
                        @item.Price
                    </h5>
                    <p class="card-text">
                        @item.Description
                    </p>
                    @Html.ActionLink("Join", "Create", "Attendee", new { partyGuid = item.PartyGuid }, new { @class = "btn btn-outline-dark" })
                </div>
                <div class="card-footer text-muted">
                    @item.PartyStart.ToShortDateString()
                </div>
            </div>
        </div>
    </div>
}