using Everlast.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace Everlast.CRUD
{
    public class PortalCRUD
    {
        public int Register(MemberViewModel model)
        {
            // create member
            return 0;
        }
        public int Login(MemberViewModel model)
        {
            // test login credentials of member
            return 0;
        }

        public int Logout(int userId)
        {
            // log the user out
            return 0;
        }
    }
}